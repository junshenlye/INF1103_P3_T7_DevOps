services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATA_FILE: /app/data/schedules.json
    volumes:
      - ./data:/app/data
    stdin_open: true
    tty: true
